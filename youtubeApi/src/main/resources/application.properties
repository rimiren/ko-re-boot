# ① .env 를 추가 구성 파일로 불러온다
spring.config.import=optional:file:.env[.properties]

# ② 다른 설정은 그대로
server.port=8080
spring.application.name=youtubeApi

# ③ 서비스 코드에서 주입받는 키
youtube.api.key=${youtube.api.key}